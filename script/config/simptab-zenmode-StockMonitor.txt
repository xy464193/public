{"theme":"random","size":"normal","time":{"color":"#fff","display":"false"},"day":{"color":"#fff","display":"false"},"devices":{"color":"#fff","display":"false"},"topsites":{"display":"false"},"css":"#stock-dashboard {\n    position: fixed;\n    top: 0;\n    left: 0;\n    width: 100vw;\n    height: 100vh;\n    color: rgba(255, 255, 255, 0.9);\n    font-family: -apple-system, BlinkMacSystemFont, \"Segoe UI\", Roboto, sans-serif;\n    pointer-events: none;\n    z-index: 50;\n}\n.stock-ui-layer {\n    position: absolute;\n    top: 50%;\n    left: 50%;\n    transform: translate(-50%, -50%);\n    width: 800px;\n    display: flex;\n    flex-direction: column;\n    align-items: center;\n    pointer-events: auto;\n}\n.stock-header {\n    width: 100%;\n    display: flex;\n    justify-content: space-between;\n    align-items: baseline;\n    margin-bottom: 30px;\n    text-shadow: 0 4px 20px rgba(0,0,0,0.2);\n}\n.stock-name {\n    font-size: 54px;\n    font-weight: 800;\n    letter-spacing: -1px;\n}\n.stock-price {\n    font-size: 72px;\n    font-weight: 200;\n    font-family: \"SF Mono\", monospace;\n}\n.stock-canvas-container {\n    width: 100%;\n    height: 350px;\n    filter: drop-shadow(0 15px 40px rgba(0,0,0,0.2));\n}\n#stock-canvas {\n    width: 100%;\n    height: 100%;\n}\n.stock-search-floating {\n    position: fixed;\n    bottom: 40px;\n    left: 50%;\n    transform: translateX(-50%);\n    display: flex;\n    gap: 10px;\n    background: rgba(255,255,255,0.08);\n    backdrop-filter: blur(15px);\n    padding: 10px 20px;\n    border-radius: 40px;\n    border: 1px solid rgba(255,255,255,0.1);\n    transition: all 0.4s;\n    opacity: 0.1;\n}\n.stock-search-floating:hover { opacity: 1; }\n.stock-search-floating input {\n    background: transparent;\n    border: none;\n    color: white;\n    outline: none;\n    width: 180px;\n    font-size: 14px;\n}\n.stock-search-floating button {\n    background: transparent;\n    border: none;\n    color: #19CAAD;\n    font-weight: bold;\n    cursor: pointer;\n    padding: 0 10px;\n}\n.stock-up { color: #ff5252 !important; }\n.stock-down { color: #4caf50 !important; }","version":"1.2.1","script":"let currentStock = localStorage.getItem('zen-stock-code') || 'sh600519';\nconst dashboardTmpl = `\n<div id=\"stock-dashboard\">\n    <div class=\"stock-ui-layer\">\n        <div class=\"stock-header\">\n            <span class=\"stock-name\">--</span>\n            <span class=\"stock-price\">--.--</span>\n        </div>\n        <div class=\"stock-canvas-container\">\n            <canvas id=\"stock-canvas\"></canvas>\n        </div>\n    </div>\n    <div class=\"stock-search-floating\">\n        <input type=\"text\" id=\"stock-input\" placeholder=\"代码 (如 600519)\">\n        <button id=\"stock-btn\">定位追踪</button>\n    </div>\n</div>`;\n$('body').append(dashboardTmpl);\nfunction drawChart(points, isUp) {\n    const canvas = document.getElementById('stock-canvas');\n    const ctx = canvas.getContext('2d');\n    const rect = canvas.parentNode.getBoundingClientRect();\n    canvas.width = rect.width * 2;\n    canvas.height = rect.height * 2;\n    ctx.scale(2, 2);\n    const width = rect.width;\n    const height = rect.height;\n    const color = isUp ? '#ff5252' : '#4caf50';\n    const vals = points.map(p => parseFloat(p.price));\n    if (vals.length === 0) return;\n    const max = Math.max(...vals);\n    const min = Math.min(...vals);\n    const range = (max - min) || 1;\n    ctx.clearRect(0, 0, width, height);\n    ctx.beginPath();\n    ctx.strokeStyle = color;\n    ctx.lineWidth = 4;\n    ctx.lineJoin = 'round';\n    points.forEach((p, i) => {\n        const x = (i / 241) * width;\n        const y = height - ((parseFloat(p.price) - min) / range) * height;\n        if (i === 0) ctx.moveTo(x, y); else ctx.lineTo(x, y);\n    });\n    ctx.stroke();\n    const grad = ctx.createLinearGradient(0, 0, 0, height);\n    grad.addColorStop(0, color + '55');\n    grad.addColorStop(1, 'transparent');\n    ctx.lineTo((points.length - 1) / 241 * width, height);\n    ctx.lineTo(0, height);\n    ctx.fillStyle = grad;\n    ctx.fill();\n}\nfunction updateStock() {\n    window.parent.postMessage({ type: 'SANDBOX_FETCH', id: 'stock_info', url: 'https://qt.gtimg.cn/q=' + currentStock, charset: 'gbk' }, '*');\n    window.parent.postMessage({ type: 'SANDBOX_FETCH', id: 'stock_minute', url: 'https://web.ifzq.gtimg.cn/appstock/app/minute/query?code=' + currentStock }, '*');\n}\nwindow.addEventListener('message', (e) => {\n    if (e.data.type === 'SANDBOX_FETCH_RESULT') {\n        if (e.data.id === 'stock_info' && e.data.data) {\n            const data = e.data.data.split('~');\n            if (data.length < 10) return;\n            $('.stock-name').text(data[1]);\n            $('.stock-price').text(parseFloat(data[3]).toFixed(2)).removeClass('stock-up stock-down').addClass(parseFloat(data[31]) >= 0 ? 'stock-up' : 'stock-down');\n        }\n        if (e.data.id === 'stock_minute' && e.data.data) {\n            try {\n                const res = JSON.parse(e.data.data);\n                const stockData = res.data[currentStock];\n                // 兼容不同 API 路径 (data.data 或 minute.data)\n                const minuteData = (stockData.data && stockData.data.data) || (stockData.minute && stockData.minute.data);\n                if (minuteData) {\n                    const points = minuteData.map(d => ({ price: d.split(' ')[1] }));\n                    drawChart(points, $('.stock-price').hasClass('stock-up'));\n                }\n            } catch (err) { console.warn('Parse minute data failed', err); }\n        }\n    }\n});\n$('#stock-btn').on('click', () => {\n    let val = $('#stock-input').val().trim().toLowerCase();\n    if (val) {\n        if (!isNaN(val) && val.length === 6) val = (val.startsWith('6') ? 'sh' : 'sz') + val;\n        currentStock = val; localStorage.setItem('zen-stock-code', currentStock); updateStock();\n    }\n});\nupdateStock(); setInterval(updateStock, 60000);"}
